name: Deprecated - Bunny Edge Script Backend (disabled)

on:
  workflow_dispatch:  # keep file for history; no automatic runs

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated and intentionally disabled."
